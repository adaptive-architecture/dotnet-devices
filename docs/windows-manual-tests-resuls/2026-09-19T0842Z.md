# Windows Manual Tests — Results, 2026-09-19T0842Z

Machine: Windows 11. Binary: AOT publish of `samples/Devices.Samples`
(`publish-samples.sh -r win-x64`, run from its own folder). Queues: Microsoft
Print to PDF (paused) and EPSON33D359 (L6270 Series, real driver).

## Prior sessions (previously only noted in `windows-manual-tests.md`)

- **Five-job `queue-sweep` on real Windows hardware**: `document.pdf` with
  printer defaults, two PNG and two JPEG jobs varying colour mode,
  orientation and scaling — all printed correctly through a spooler queue.
  Covers the submission path end to end: device mode from options, GDI page,
  PDF rendered to PNG and drawn, job reaching `Completed`. Explicitly does
  not cover the rest of the plan, in particular never T10 (a `spooler://`
  queue never converts to PWG Raster).

## Pass

- **T2.1 paused** (Epson): `Paused (PRINTER_STATUS_PAUSED)`, not accepting.
- **T2.4 Use Printer Offline** (Epson): `Offline
  (PRINTER_ATTRIBUTE_WORK_OFFLINE)`, `IsAcceptingJobs: False`.
- **T3 sample print cycle** (Epson): `Job 2`, `Copy 1 of 2…`,
  `DroppedOptions: MediaType, OutputBin, PageRanges, NumberUp` — exactly the
  four. Queue window showed both jobs; properties agreed (Main tray, A4).
  Plain-text RAW jobs queue but leave no marks (firmware reads only its own
  language — expected); a JPEG through the GDI path printed `Completed 1/1`.
- **T5 error paths**: missing queue → `InvalidOperationException`, Win32 1801
  with text; cancel unknown job → `false`.
- **T6 Print to PDF**: whole-word names, exact numbers (Letter=1, A4=9), no
  trays + empty default source (consistent), 600 dpi, colour, no duplex.
- **T6 Epson**: whole-word names, `Main=257`, 300/600 dpi, duplex + colour,
  defaults A4/Main/Portrait/300.
- **T7 access**: `PRINTER_ACCESS_USE` proved for status, queue read, cancel
  and submit on both queues.
- **T4 AOT complete**: every `ISpoolerDriver` method ran from the
  `win-x64/aot` binary — Print-tab discovery (`EnumeratePrintersAsync`),
  correlate with identity (`GetIdentityAsync`, no crash), spooler checks
  (`GetJobs`, `GetStatus`, `GetConfiguration`, `CancelJob`, `Submit`) and a
  submit + watch to `Completed 1/1` (`GetJobAsync` via the monitor, Job 5).
  Bonus: a monochrome job printed a non-colour page (colour mode reaches the
  device mode on a real driver).
- **Identity case 3 (WSD port)**: queue port is
  `WSD-d607cf40-f481-4275-b301-1f40ced6385e`; correlate correctly kept
  `spooler://EPSON33D359…` as its own device — pass. This also explains
  T2.2/T2.3: a WSD monitor raises no offline/error bits.
  Open observation: correlate kept `spooler://EPSON33D359…` as its own
  device instead of merging it with the `raw://cfe92100-…` channel — the
  queue's port name did not resolve to that key. Follow up against the
  identity cases with the actual port spelling.

## Unproducible on this connection, not failed

- **T2.2 physical offline** (device off): spooler and queue window both stay
  `Idle`; no `PRINTER_STATUS_OFFLINE` bit is raised. Nothing to map.
- **T2.3 passive error** (cover open, tray out): spooler and Windows UI both
  stay `Idle`; the driver surfaces faults only in its own monitor. Mapping
  covered by `WindowsSpoolerStatusMapperTests`; T2.4 proves the `Offline`
  state path end to end.
- **T1 multi-job layout, T8 failed-open**: settled by CI interop + seam
  tests; queues were empty in every run.

- **T6 Epson defaults follow the dialog**: after switching orientation in
  Printing Preferences, the check moved `Portrait` → `Landscape`
  (`DefaultOrientation: Landscape`, rest unchanged) — pass.

## Remaining

- T3 variants the sample button skips: unreported media/tray names,
  `Copies=3`, `ResolutionDpi+Quality`, failed write, short DEVMODE.
- T5.4 spooler stopped (~1722, never empty list).
- Identity cases 1–7 + `IsDefault`/`IsShared`.
- **T9 PDF via spooler (pass, minus corrupt)**: `pdf-spooler.json` set, one
  job each in the queue window (Jobs 6, 7), progress `0/4→4/4` and `0/2→2/2`.
  Full set printed all four pages; `2,4` printed exactly pages 2 and 4, so
  the converter selected the range. Sheets came out 4,3,2,1 and 4,2 because
  the driver default is Back to Front — driver behavior, not a fault.
  Left: `corrupt.pdf` alone must throw and leave the queue empty.
- **T9 corrupt PDF (pass, with a wording note)**: sent alone, failed as
  `InvalidOperationException` surfaced through `PrintJobRunner`
  (`Nothing was sent: The PDF could not be read…`), queue window empty
  throughout. Note: the filename rides the job outcome, not the exception
  text (`WindowsPdfRenderer.cs:99-102`) — possible follow-up.
- T10 PWG Raster over IPP (never run anywhere).
